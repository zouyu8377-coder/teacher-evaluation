# 教师评价考核平台 - 已改进项
## 一、功能开发完成记录
### 2026年4月15日完成（持续更新）
✅ **考试时间校验增强**：
- 修改 `ActivityManage.vue` 和 `ActivityDetail.vue` 的编辑表单
- C级活动考试开始时间必须在报名截止之后，否则阻断操作并提示"考试时间应在报名结束之后"
- 后端 `ActivityService.java` 已有校验逻辑，本次加强前端验证

✅ **活动状态保持**：
- 修复 `ActivityService.java` 的 `update` 方法
- 对活动的任何数据进行操作后，状态不再自动变化（之前会被重置为"草稿"或"已关闭"）
- 移除了 update 时自动设置 hasExam 和 status 的逻辑

✅ **教师端时间显示优化**：
- 修改 `teacher/ActivityDetail.vue`：C级显示"考试时间"，非C级显示"上传窗口"
- 修改 `teacher/Enrollment.vue`：表格列根据活动级别显示对应的时间信息
- 不再显示"考核时间"，改为显示精确的考试时间或上传窗口时间
- 后端 `ActivityController.java` 已返回 examStart、examEnd、materialStart、materialEnd 字段

✅ **评分人数自动同步**：
- 修改 `ActivityManage.vue` 的 `handleSaveReviewerConfig` 函数
- 评分人数自动等于勾选的评分员数量，不再需要手动输入
- 修复创建活动时默认评分人数为0的问题

✅ **评分权限校验增强**：
- 后端 `EvaluationService.validateEvaluatorPermission` 添加详细日志
- 考核员只能对分配给自己的活动进行评分
- 解析 reviewerIds JSON 时增加错误信息输出

✅ **考核员活动列表功能增强**：
- 增加"所属教研组"字段，显示教师所属部门
- 增加"提交时间"字段，根据活动级别显示文档上传时间或考试提交时间
- C级考核只有提交答卷后才显示"查看答卷"入口（submissionStatus === 'submitted'）
- 其他级别只有已上传文档才显示"查看文档"入口
- 增加"已打分"列，显示当前评分员给该教师的打分（调用 `getTeacherActivityEvaluations` 获取）

✅ **查看答卷体验优化**：
- 弹窗宽度扩大到 95%，几乎全屏显示
- 不再跳转到独立页面，直接在当前页面弹窗显示
- 保留与 ExamRecords.vue 相同的题目详情样式

✅ **后端 API 增强**：
- `GET /api/activities/{id}/enrollments` 返回字段增加：department、submittedAt、submissionStatus、examRecordId
- 新增 `DocumentService.getLatestDocument` 方法，获取教师最新上传的文档

✅ **前端问题修复**：
- 修复 `el-checkbox` 组件 `:label` 弃用警告，改为使用 `:value`
- 修复评分人数默认值为2的问题，改为0

### 2026年4月14日完成
✅ **学习资料上传功能**：
- 后端已有 `LearningMaterialController` API，支持上传/下载/删除学习资料
- 前端活动详情页新增"学习资料"模块，管理员可上传、下载、删除资料
- 教师端"学习资料"页面可按活动筛选并下载资料
- 侧边栏菜单添加"学习资料"入口

✅ **FormData 上传 Content-Type 修复**：
- 修复前端 axios 拦截器对 FormData 请求错误设置 Content-Type 的问题
- 修改 `frontend/src/api/index.ts`，当检测到 FormData 时跳过 Content-Type 设置
- 问题原因：强制设置 `application/json` 导致后端无法识别 multipart 请求

✅ **项目目录清理**：
- 删除 VKE/K8s 部署相关脚本（deploy/vke/、direct_deploy.sh 等）
- 删除 sealos 配置（.sealos/、sealos/）
- 删除旧测试脚本（test-api*.js、test-*.ps1 等）
- 保留 Docker Compose 部署配置

### 2026年4月12日完成
✅ **全局异常处理器**：新增`exception/GlobalExceptionHandler.java`和`exception/BusinessException.java`，统一异常响应格式，添加异常日志记录，业务异常返回400状态码符合RESTful规范
✅ **活动删除关联检查**：新增`ActivityService.validateDelete()`方法，检查活动是否有报名记录或评分记录，有数据的活动不可删除
✅ **评分权限校验**：新增`EvaluationService.validateEvaluatorPermission()`方法，校验考核员是否被分配到对应活动，解析JSON格式的reviewerIds
✅ **成绩发布逻辑bug修复**：修复按教师分组计算平均分的逻辑，修复循环中使用错误变量的bug，添加日志记录
✅ **级别结构修改为并行级别**：修改`Activity.Level`枚举，改为C→B2/B1→A2/A1的并行级别结构，修改报名资格校验逻辑
✅ **教师个人首页功能**：新增`dto/TeacherDashboardDTO.java`、`service/TeacherDashboardService.java`、`controller/TeacherDashboardController.java`，前端新增教师首页页面，展示基本信息、当前级别、待办事项、当前报考、历史记录、快捷操作，登录后默认跳转到教师首页
✅ **登录错误提示优化**：完善错误密码、账号禁用等场景的错误提示，修复前端未显示明确错误信息的问题，优化教师登录后默认跳转至个人首页

### 2026年4月11日完成
✅ **管理员用户管理编辑功能**：
- 前端添加编辑按钮和数据填充逻辑
- 修改`handleSubmit`方法，区分新增和编辑模式
- 修复`handleDelete`方法，调用后端删除API
- 添加API返回类型定义，解决TypeScript类型错误
- 重新构建前端镜像并部署
✅ **评分人数重置问题修复**：
- 后端`ActivityService.java`的update方法添加判断逻辑，只有当新值不是默认值2，或原值为null时才更新reviewerCount
- 前端`ActivityManage.vue`修正reviewerCount取值逻辑，避免0被错误替换为默认值
✅ **教师报名列表显示优化**：
- 前端添加`getAvailableActivitiesForTeacher`到导入语句，使用正确的API
- 后端`getAvailableForTeacher`方法添加过滤已报名活动的逻辑，已报名活动不再出现在可报名列表
✅ **筛选下拉框显示优化**：为所有`el-select`组件添加`filterable`属性，支持搜索筛选
✅ **考核活动操作栏优化**：增加列宽、缩短按钮文字、添加flex布局，解决操作栏按钮换行问题
✅ **关闭活动功能修复**：后端update方法修改为只更新非null字段，避免关闭活动时覆盖其他字段
✅ **级别顺序优化**：修复级别高低顺序问题，C级为最低级别，A1级为最高级别
✅ **权限检查优化**：修改API响应拦截器，对403状态码不显示弹窗提示，只在控制台输出警告信息
✅ **级别统计优化**：去掉级别统计卡片中的"初级、中级、高级"等字样，只保留级别徽章和通过人数的显示

### 2026年4月9日完成
✅ **教师无法查看开放中活动问题修复**：
- 修改`JwtAuthenticationFilter.java`，从数据库加载完整的User对象并存储到SecurityContext
- 修复`@AuthenticationPrincipal User user`获取为null的问题
- 更新数据库活动状态，将draft状态改为active
✅ **报名功能修复**：新增`POST /api/activities/{id}/enroll`报名API
✅ **活动列表优化**：后端`/api/activities`新增`activeOnly`参数，前端"我的文档"活动筛选只显示进行中的活动
✅ **权限检查问题修复**：修复登录后首页弹窗提示无权限的问题

### 2026年4月6日完成
✅ **活动级别功能开发**：
- 新增`activities`表，支持C/B2/B1/A2/A1五级考核
- 新增活动管理功能，支持设置级别、容量、报名时间
- 新增报名资格校验逻辑，只有通过前置级别才能报名更高等级
- 前端支持按级别筛选活动，报名时自动校验资格
✅ **考试系统开发**：
- 新增题库管理功能，支持单选/多选题型
- 新增试卷管理功能，支持手动/随机选题
- 新增在线考试功能，支持断点续考、自动保存答案、自动判分
- 新增考试记录管理功能，支持查看考试成绩和答题记录

### 2026年4月3日完成
✅ **考核报名功能开发**：
- 新增`period_enrollments`报名表
- 新增教师报名功能，报名后才能下载学习资料、上传作业文档、被考核员评分
- 管理员在考核周期页可以查看已报名的教师，并踢出教师
- 考核员页面改为考核周期维度，只能查看已报名教师的作业和评分
✅ **学习资料功能开发**：
- 新增`learning_materials`表
- 管理员可以上传学习资料，所有角色可以下载
- 资料按考核周期分类
✅ **文档上传问题修复**：
- 修复`DocumentUpload.vue`中表单验证问题，将file字段放入form响应式对象中
- 支持大文件上传（最大100MB）
✅ **Docker环境搭建完成**：
- 配置国内镜像源，解决Docker Hub无法访问问题
- 成功拉取MySQL、Redis、MinIO、JRE基础镜像
- Docker Compose启动所有服务成功
✅ **前端登录问题修复**：
- 修改前端代理配置，指向正确的后端端口
- 修复密码提示错误问题，更新登录页面的测试账号密码
- 修复CORS跨域配置，允许前端地址访问
✅ **数据库初始化问题修复**：
- 新增`DataInitializer.java`，应用启动时自动创建测试用户和默认考核周期
- 修复测试密码BCrypt哈希不匹配问题

### 项目初期完成（2024年）
✅ **项目架构设计与技术选型**
✅ **数据库设计与SQL脚本**
✅ **API接口协议定义**
✅ **前后端开发任务分解**
✅ **后端基础框架搭建（Spring Boot + Security + JWT + JPA）**
✅ **前端基础框架搭建（Vue3 + Vite + Element Plus + Pinia + Axios）**
✅ **Docker部署配置**
✅ **项目文档编写**

## 二、问题修复汇总
| 问题描述 | 修复时间 | 修复状态 |
|----------|----------|----------|
| Docker网络问题，无法拉取镜像 | 2026-04-03 | ✅ 已修复（配置国内镜像源） |
| 端口冲突，8080/5173被占用 | 2026-04-03 | ✅ 已修复（调整端口配置） |
| 数据库方言不匹配，H2无法执行MySQL语法 | 2026-04-03 | ✅ 已修复（显式配置Hibernate使用H2方言） |
| 测试账号无法登录 | 2026-04-03 | ✅ 已修复（创建DataInitializer自动初始化数据） |
| 前端服务无法访问 | 2026-04-03 | ✅ 已修复（创建批处理文件正确启动，修复代理配置） |
| 登录显示"无权限访问" | 2026-04-03 | ✅ 已修复（修复Login.vue密码提示，更新CORS配置） |
| 教师无法查看开放中活动 | 2026-04-09 | ✅ 已修复（修复JWT认证过滤器） |
| 报名功能失败 | 2026-04-09 | ✅ 已修复（新增报名API） |
| 登录后首页弹窗提示无权限 | 2026-04-09 | ✅ 已修复（修改API响应拦截器） |
| 教师报名列表显示已报名活动 | 2026-04-11 | ✅ 已修复（添加过滤逻辑） |
| 编辑活动时评分人数被重置为2 | 2026-04-11 | ✅ 已修复（添加判断逻辑，只有用户修改时才更新） |
| 活动删除时没有关联检查 | 2026-04-12 | ✅ 已修复（添加关联检查逻辑） |
| 评分权限没有校验 | 2026-04-12 | ✅ 已修复（添加考核员权限校验） |
| 成绩发布计算逻辑错误 | 2026-04-12 | ✅ 已修复（修复平均分计算逻辑） |
| 错误密码登录无明确提示 | 2026-04-12 | ✅ 已修复（完善错误提示逻辑，显示"用户名或密码错误"） |
| 学习资料上传500错误 | 2026-04-14 | ✅ 已修复（FormData Content-Type 修复） |
| 项目目录混乱 | 2026-04-14 | ✅ 已修复（清理无用脚本） |
| 评分人数与实际勾选人数不符 | 2026-04-14 | ✅ 已修复（自动同步为勾选数量） |
| 评分员无权评分报错 | 2026-04-14 | ✅ 已修复（配置 reviewerIds 为正确的评分员 ID） |
| 考核员活动列表缺少教研组字段 | 2026-04-14 | ✅ 已修复（后端返回 department 字段） |
| 考核员活动列表缺少提交时间 | 2026-04-14 | ✅ 已修复（后端根据级别获取提交时间） |
| 未提交也能查看答卷 | 2026-04-14 | ✅ 已修复（检查 submissionStatus 后才显示入口） |
| 查看答卷跳转页面 | 2026-04-14 | ✅ 已修复（改为弹窗显示，宽度95%） |
| 考核员看不到已打分分数 | 2026-04-14 | ✅ 已修复（增加已打分列） |
| 考试时间在报名截止之前 | 2026-04-15 | ✅ 已修复（前端校验+后端校验阻断） |
| 操作活动数据后状态被重置 | 2026-04-15 | ✅ 已修复（移除自动更新状态逻辑） |
| 教师端显示考核时间 | 2026-04-15 | ✅ 已修复（改为考试时间/上传窗口） |

## 三、优化完成记录
✅ **前端日志统一管理**：新增`frontend/src/utils/logger.ts`，支持DEBUG/INFO/WARN/ERROR四个级别，生产环境自动过滤DEBUG/INFO级别，统一日志格式化
✅ **API拦截器优化**：请求拦截记录所有API请求的方法、URL、参数，响应拦截记录响应状态和数据，错误拦截记录完整的错误信息，添加网络错误友好提示
✅ **异常静默吞掉问题修复**：修复`frontend/src/components/AppHeader.vue`登出API调用失败没有日志的问题，添加异常日志记录
✅ **API响应类型统一**：在`frontend/src/api/index.ts`中导出通用`ApiResponse<T>`类型，统一所有API返回类型定义
✅ **数据初始化优化**：应用启动时自动创建测试用户、默认考核周期、默认活动、默认题库和试卷，无需手动导入SQL
✅ **Docker镜像优化**：优化Dockerfile，直接使用本地jar包，避免重复编译超时，减小镜像体积
✅ **跨域配置优化**：支持多个前端地址访问，方便开发和测试环境切换

✅ **Docker镜像体积优化** (2026-04-17):
- 后端：添加 `backend/.dockerignore`，排除 IDE、文档、测试代码等
- 后端：优化 Dockerfile，使用 Maven 镜像构建，本地已有 jar 时直接复制（避免重复编译）
- 前端：完善 `frontend/.dockerignore`，排除 node_modules、构建产物、文档等
- 优化效果：后端预计从 432MB 降至 ~150MB，前端保持约 95MB

## 四、功能完成率统计
| 功能模块 | 总功能数 | 已完成数 | 完成率 |
|----------|----------|----------|--------|
| 用户与权限系统 | 6 | 6 | 100% |
| 学习资源模块 | 4 | 4 | 100% |
| 报名系统 | 5 | 5 | 100% |
| 考试系统 | 7 | 7 | 100% |
| 主观材料评分系统 | 3 | 3 | 100% |
| 成绩与反馈系统 | 5 | 5 | 100% |
| **合计** | **30** | **30** | **100%** |