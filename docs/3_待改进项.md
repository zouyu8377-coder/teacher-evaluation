# 教师评价考核平台 - 待改进项
## 一、按优先级分类
### 🟡 中优先级（2周内完成）
| 序号 | 改进内容 | 问题说明 | 预计工作量 |
|------|----------|----------|------------|
| 1 | Docker健康检查配置 | 所有容器缺少健康检查配置，无法自动识别服务异常并重启 | 30分钟 |
| 2 | 幂等性校验完善 | 部分接口无幂等校验，重复提交可能导致数据重复 | 1小时 |
| 3 | 后端日志完善 | Service层日志已添加，需要进一步完善请求日志、异常日志、操作审计日志 | 2小时 |
| 4 | API版本控制 | 预留API版本控制（如`/api/v1/`），便于后续平滑升级 | 1小时 |
| 5 | 热点数据Redis缓存 | 用户权限、配置信息、活动列表等热点数据增加Redis缓存，提升系统性能 | 2小时 |
| 6 | 系统集成测试与回归测试 | 对所有功能进行完整测试，修复潜在bug | 2人天 |
| 7 | 性能优化与安全加固 | 压测系统性能，修复安全漏洞，优化慢查询 | 2人天 |

### 🔵 低优先级（版本迭代完成）
| 序号 | 改进内容 | 问题说明 | 预计工作量 |
|------|----------|----------|------------|
| 1 | TypeScript类型完善 | 多处使用`any`类型，需要逐步为各个模块定义具体接口类型 | 4小时 |
| 2 | 请求重试机制 | 网络异常时缺少自动重试逻辑，可使用axios的retry拦截器 | 1小时 |
| 3 | 数据脱敏 | 用户列表等接口返回的敏感字段（如手机号、身份证号）应脱敏处理 | 1小时 |
| 4 | 操作日志审计功能 | 记录用户的关键操作，便于审计和问题排查 | 1人天 |
| 5 | 敏感信息外部化配置 | 当前数据库密码、JWT密钥、MinIO访问密钥硬编码在配置文件中，存在安全风险 | 1小时 |
| 6 | 配置文件加密 | 敏感信息以明文存储在配置文件中，容易泄露 | 2小时 |

## 二、具体改进方案参考
### 1. Docker健康检查配置
在`docker-compose.yml`中添加健康检查：
```yaml
services:
  mysql:
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
  redis:
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
  minio:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
  backend:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
```

### 2. 敏感信息外部化配置
修改`application.yml`配置文件，使用环境变量注入：
```yaml
spring:
  datasource:
    password: ${DB_PASSWORD:root123}
  data:
    redis:
      password: ${REDIS_PASSWORD:}
jwt:
  secret: ${JWT_SECRET:teacher-eval-secret-key-2024-long-enough-for-security}
  expiration: 86400000
minio:
  accessKey: ${MINIO_ACCESS_KEY:minioadmin}
  secretKey: ${MINIO_SECRET_KEY:minioadmin}
```

## 三、待办事项跟踪
| 序号 | 任务 | 状态 | 预计完成时间 | 负责人 |
|------|------|------|--------------|--------|
| 1 | Docker健康检查配置 | 待开始 | 2026-04-20 | 运维 |
| 2 | 系统集成测试 | 待开始 | 2026-04-25 | 测试人员 |
| 3 | 性能优化与安全加固 | 待开始 | 2026-04-30 | 运维/开发 |